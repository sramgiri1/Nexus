# Agent Registry

P45 introduces a metadata-only registry for NEXUS agent identities. It describes
agent roles, capabilities, path/data boundaries, approval requirements, evidence
requirements, cost posture, memory posture, and handoff rules.

This directory does not grant runtime permissions. It does not enable provider
dispatch, tool execution, DB writes, worker runtime, source mutation, release
execution, or agent self-update.
