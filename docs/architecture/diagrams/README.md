# Diagram Registry

This directory tracks architecture diagrams and placeholders without requiring every diagram artifact to exist yet.

Use `diagram-registry.json` as the index for:

- diagram id
- title
- purpose
- status
- source path if present
- image path if present
- owner
- public-safe flag
- last updated phase

Planned diagrams should stay in the registry even when the underlying asset does not exist yet.
