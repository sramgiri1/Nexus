# ShiftPay

> Know your take-home before accepting a shift.

Stack: Node.js 20, Fastify 4, Prisma 5, PostgreSQL (Supabase), iOS 16+ SwiftUI

Status: Pre-Sprint — Gate G1
Agents: ATLAS, FORGE, CORE, BEACON, ORACLE

## Quick Start (when backend is ready)
```bash
cd backend && cp .env.example .env
npm install && npx prisma migrate dev
npm run dev
```
